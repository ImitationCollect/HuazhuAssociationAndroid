import React from 'react';
import {StyleSheet, Text, useColorScheme, View, ScrollView} from 'react-native';

function Strolling() {
  return (
    <ScrollView>
      <Text>Strolling</Text>
    </ScrollView>
  );
}

export default Strolling;
